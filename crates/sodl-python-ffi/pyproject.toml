[build-system]
requires = ["maturin>=1.6,<2.0"]
build-backend = "maturin"

[project]
name = "sodl-native"
version = "0.1.1"
description = "Rust pyo3 bridge for SODL blob and compression primitives"
requires-python = ">=3.10"

[tool.maturin]
module-name = "sodl_native"
bindings = "pyo3"
